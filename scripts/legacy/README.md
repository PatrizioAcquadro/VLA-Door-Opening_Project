# Legacy VLA-LEGO Scripts

These scripts exercise the retained LEGO baseline under `sim/lego`. They are not active VLA-Door-Opening validation commands.

Use active door-opening validation instead:

```bash
python scripts/validate_door_scene.py
python scripts/validate_mujoco.py
python scripts/validate_assets.py
```
