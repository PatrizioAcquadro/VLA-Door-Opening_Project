# Legacy Baseline Archive

This directory contains the historical LEGO manipulation baseline that the repo
was bootstrapped from. It is retained for provenance and comparison only.

Active door-opening work should use the live `sim/`, `configs/sim/door.yaml`,
`data/`, `models/`, `train/`, `eval/`, and `tests/` paths outside this archive.
